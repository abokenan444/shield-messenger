<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- Header -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        android:fitsSystemWindows="true"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Back Button -->
        <FrameLayout
            android:id="@+id/backButton"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:layout_marginStart="8dp"
            android:background="@drawable/top_icon_bg"
            android:clickable="true"
            android:focusable="true">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_back_arrow"
                android:contentDescription="Back" />

        </FrameLayout>

        <!-- Title -->
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Delete Account"
            android:textSize="22sp"
            android:textColor="#CCCCCC"
            android:fontFamily="@font/space_grotesk_bold"
            android:textStyle="bold"
            android:gravity="center" />

        <!-- Settings Button (invisible for balance) -->
        <FrameLayout
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:layout_marginEnd="8dp"
            android:visibility="invisible" />

    </LinearLayout>

<ScrollView
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:fillViewport="true"
    app:layout_constraintTop_toBottomOf="@id/header"
    app:layout_constraintBottom_toTopOf="@id/bottomNav">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="16dp"
        android:paddingBottom="24dp">

            <!-- Password Confirmation -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Confirm Account Wipe"
                android:textSize="13sp"
                android:textColor="#888888"
                android:fontFamily="@font/poppins_medium"
                android:layout_marginBottom="8dp" />

            <EditText
                android:id="@+id/wipePasswordInput"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:background="@drawable/lock_password_input_bg"
                android:hint="Enter your password to confirm"
                android:inputType="textPassword"
                android:textColorHint="#666666"
                android:textColor="#FFFFFF"
                android:textSize="15sp"
                android:fontFamily="@font/poppins_regular"
                android:paddingStart="19dp"
                android:paddingEnd="19dp"
                android:layout_marginBottom="20dp" />

            <!-- Type DELETE Confirmation -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Type &quot;DELETE&quot; to confirm"
                android:textSize="13sp"
                android:textColor="#888888"
                android:fontFamily="@font/poppins_medium"
                android:layout_marginBottom="8dp" />

            <EditText
                android:id="@+id/wipeConfirmInput"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:background="@drawable/lock_password_input_bg"
                android:hint="Type DELETE in capital letters"
                android:textColorHint="#666666"
                android:textColor="#FFFFFF"
                android:textSize="15sp"
                android:fontFamily="@font/poppins_regular"
                android:paddingStart="19dp"
                android:paddingEnd="19dp"
                android:layout_marginBottom="20dp" />

            <FrameLayout
                android:id="@+id/wipeAccountButton"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:background="@drawable/neon_blue_button_bg"
                android:clickable="true"
                android:focusable="true"
                android:layout_marginBottom="24dp">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:text="Wipe Account"
                    android:textSize="16sp"
                    android:textColor="#FFFFFF"
                    android:fontFamily="@font/space_grotesk_bold"
                    android:textStyle="bold" />

            </FrameLayout>

            <!-- Info Box -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:background="@drawable/wallet_token_bg"
                android:padding="20dp">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Before You Proceed"
                    android:textSize="16sp"
                    android:textColor="#FFFFFF"
                    android:fontFamily="@font/poppins_medium"
                    android:layout_marginBottom="8dp" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Make sure you have backed up your seed phrase to access funds or transfer out. You will lose your contacts and will need to readd them as you will have to create a new identity."
                    android:textSize="13sp"
                    android:textColor="#888888"
                    android:fontFamily="@font/poppins_regular"
                    android:lineSpacingMultiplier="1.4" />

            </LinearLayout>

    </LinearLayout>

</ScrollView>

<!-- Bottom Navigation -->
<include
    android:id="@+id/bottomNav"
    layout="@layout/bottom_navigation"
    android:layout_width="match_parent"
    android:layout_height="110dp"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
