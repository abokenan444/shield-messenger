<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Android 12+ Splash Screen Theme -->
    <style name="Theme.SecureLegion" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorPrimaryVariant">@color/accent_blue</item>
        <item name="colorOnPrimary">@color/text_white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/accent_blue</item>
        <item name="colorSecondaryVariant">@color/primary_blue</item>
        <item name="colorOnSecondary">@color/text_white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/darker_bg</item>
        <!-- Background colors -->
        <item name="android:windowBackground">@color/dark_bg</item>
        <item name="android:colorBackground">@color/dark_bg</item>
        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/text_white</item>
        <item name="android:textColorSecondary">@color/text_gray</item>

        <!-- Android 12+ Splash Screen -->
        <item name="android:windowSplashScreenBackground">@color/dark_bg</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/secure_logo</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/dark_bg</item>
    </style>
</resources>
