# Copy this file to keystore.properties and fill in your values.
# keystore.properties is gitignored and MUST NOT be committed.

# Release signing
storeFile=release-keystore.jks
storePassword=
keyAlias=
keyPassword=

# Jupiter Ultra API key (Solana DEX)
jupiterApiKey=

# Google Play demo account credentials (googleplaydemo flavor)
demoUsername=
demoPassword=
